import React from 'react'

const TaskNode = () => {
  return (
    // make navbar views component (because this navbar with tailwind css styled is showing not showing on the home pages, yes but will be on all the routes that start from task-node )component 
    <div>TaskNode</div>
  )
}

export default TaskNode